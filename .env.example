# VolleyPlus API - Cloudflare Workers

This project runs entirely on Cloudflare Workers.
All configuration is now in the worker/ directory.

## Local Development

Configure secrets in: worker/.dev.vars
Copy worker/.dev.vars.example to worker/.dev.vars and add your JWT_SECRET.

## Production

Use Wrangler CLI:
  cd worker
  npx wrangler secret put JWT_SECRET

## Full Documentation

See worker/README.md for complete setup instructions.

